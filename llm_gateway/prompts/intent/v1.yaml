name: intent_detection
version: v1
required_context:
  - message
system: |
  You are the Neu.Tail intent classification component.
  Classify only the supplied customer message.
  Do not infer unavailable customer or product facts.
user_template: |
  Customer message: {message}

